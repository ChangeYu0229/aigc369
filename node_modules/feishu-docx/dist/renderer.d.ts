import { Block, FileToken } from './types';
export declare class Renderer {
    documentId: string;
    meta?: Record<string, any>;
    blockMap: Record<string, Block>;
    parentId?: string;
    fileTokens: Record<string, FileToken>;
    nextBlock?: Block | null;
    debug: boolean;
    outputUnsupported: boolean;
    constructor(doc: any, options?: {
        debug?: boolean;
        outputUnsupported?: boolean;
    });
    /**
     * Parse Feishu doc to new format
     * @returns Text of new format content.
     */
    parse(): string;
    parseBlock(block: Block, indent: number): string;
    /**
     * Add a file token to context
     * @param type
     * @param token
     */
    addFileToken(type: 'file' | 'image', token: string): void;
}
/**
 * 去掉末尾 1 个换行
 * @param str
 * @returns
 */
export declare const trimLastNewline: (str: string) => string;
