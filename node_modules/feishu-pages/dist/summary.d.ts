import { Doc } from './feishu';
export interface FileDoc extends Doc {
    slug: string;
    position: number;
    filename: string;
    content?: string;
    meta?: Record<string, any>;
    fileTokens?: any;
    children: FileDoc[];
}
/**
 * Iter docs tree to give them as slug, position and filename
 * @param docs
 * @param parentSlug
 */
export declare const prepareDocSlugs: (docs: FileDoc[], slugMap: Record<string, string>, parentSlug?: string) => void;
/**
 * Generate SUMMARY.md
 */
export declare const generateSummary: (docs: FileDoc[]) => string;
